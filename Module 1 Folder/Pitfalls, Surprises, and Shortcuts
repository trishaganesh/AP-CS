/**
 * The purpose of this program is to
 * calculate the average of four grades
 * and then calculate the average of 5 more grades
 * (10 grades in total)
 *
 * @author Trisha Ganesh
 * @version 08/11/20
 */

public class GradesV2
{
    public static void main(String[] args)
    {
        //local variables
        int numTests = 0;      //counts number of tests
        int testGrade = 0;     //individual test grade
        int totalPoints = 0;   //total points for all tests
        double average = 0.0;  //average grade


        System.out.println("We will be calculating the average of 10 test scores!");
        System.out.print("\n");

        // calculate points
        testGrade = 95;   // test grade
        numTests++;
        totalPoints += testGrade;
        average = totalPoints;
        System.out.println("Test # " + numTests + "\t\tTest Grade: " + testGrade + "\t\tTotal Points: " + totalPoints + "\t\tAverage Score: " + average);


        // calculate points
        testGrade = 73;   // test grade
        numTests++;
        totalPoints += testGrade;
        average = totalPoints / 2;
        System.out.println("Test # " + numTests + "\t\tTest Grade: " + testGrade + "\t\tTotal Points: " + totalPoints + "\t\tAverage Score: " + average);

        // calculate points
        testGrade = 91;   // test grade
        numTests++;
        totalPoints += testGrade;
        average = (double) totalPoints / 3;
        System.out.println("Test # " + numTests + "\t\tTest Grade: " + testGrade + "\t\tTotal Points: " + totalPoints + "\t\tAverage Score: " + average);

        // calculate points
        testGrade = 82;   // test grade
        numTests++;
        totalPoints += testGrade;
        average = (double) totalPoints / 4;
        System.out.println("Test # " + numTests + "\t\tTest Grade: " + testGrade + "\t\tTotal Points: " + totalPoints + "\t\tAverage Score: " + average);

        System.out.println("------------------------------------------------------------------------------------------------");
        // calculate points of my own numbers
        testGrade = 89;   // test grade
        numTests++;
        totalPoints += testGrade;
        average = (double) totalPoints / 5;
        System.out.println("Test # " + numTests + "\t\tTest Grade: " + testGrade + "\t\tTotal Points: " + totalPoints + "\t\tAverage Score: " + average);

        testGrade = 99;   // test grade
        numTests++;
        totalPoints += testGrade;
        average = (double) totalPoints / 6;
        System.out.println("Test # " + numTests + "\t\tTest Grade: " + testGrade + "\t\tTotal Points: " + totalPoints + "\t\tAverage Score: " + average);

        testGrade = 71;   // test grade
        numTests++;
        totalPoints += testGrade;
        average = (double) totalPoints / 7;
        System.out.println("Test # " + numTests + "\t\tTest Grade: " + testGrade + "\t\tTotal Points: " + totalPoints + "\t\tAverage Score: " + average);

        testGrade = 78;   // test grade
        numTests++;
        totalPoints += testGrade;
        average = (double) totalPoints / 8;
        System.out.println("Test # " + numTests + "\t\tTest Grade: " + testGrade + "\t\tTotal Points: " + totalPoints + "\t\tAverage Score: " + average);

        testGrade = 98;   // test grade
        numTests++;
        totalPoints += testGrade;
        average = (double) totalPoints / 9;
        System.out.println("Test # " + numTests + "\t\tTest Grade: " + testGrade + "\t\tTotal Points: " + totalPoints + "\t\tAverage Score: " + average);

        testGrade = 100;   // test grade
        numTests++;
        totalPoints += testGrade;
        average = (double) totalPoints / 10;
        System.out.println("Test # " + numTests + "\t\tTest Grade: " + testGrade + "\t\tTotal Points: " + totalPoints + "\t\tAverage Score: " + average);

    }//end of main method
}//end of class
